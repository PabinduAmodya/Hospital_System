# ── Production Configuration (TiDB Cloud) ──

# Database Configuration (TiDB Serverless - MySQL Compatible)
spring.datasource.url=jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/hospital_db?sslMode=VERIFY_IDENTITY&enabledTLSProtocols=TLSv1.2,TLSv1.3&createDatabaseIfNotExist=true
spring.datasource.username=3XFRG4z4cvx4mXW.root
spring.datasource.password=${TIDB_PASSWORD:IF57HqZLsscztqvq}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging (reduced for production)
logging.level.org.springframework.security=WARN
logging.level.com.hospital_system=INFO

# Frontend URL (update after deploying frontend)
app.frontend-url=${FRONTEND_URL:http://localhost:5173}
